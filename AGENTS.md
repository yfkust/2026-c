# C 题项目协作约束

## 项目范围与当前状态

本项目研究 2026 年中国研究生数学建模竞赛 C 题《服务于脑机接口与精神性疾病诊断的脑电图计算模型》。五个阶段及当前状态见 `docs/PLAN.md` 和 `docs/PROGRESS.md`。不得越过阶段门禁批量实现后续工作。

新会话先读：

1. `AGENTS.md`；
2. `docs/PLAN.md`；
3. `docs/PROGRESS.md`；
4. 当前任务直接相关的资料和文件。

除非进度记录失效，不重新扫描全部文献、仓库或数据。

## 资料权威顺序

1. 原始赛题：`docs/服务于脑机接口与精神性疾病诊断的脑电图计算模型.docx`，其 Markdown 提取稿仅用于检索；
2. 原始 MAT 数据及已复核的实验事实：`data/`、`docs/DATA_SPEC.md`；
3. 整合方案：`docs/C题_解题思路_提取整合完整版.md`；
4. 其他解读稿：`docs/q1.md`、`docs/q2.md`、`docs/q3.md`、`docs/method.md`；
5. 参考文献与参考代码：`refer_paper/`、`repos/`。

论文结构和写作约束以 `docs/deadline.md` 为准。资料冲突必须记录来源、证据和处理决定；辅助材料不得覆盖赛题原文或实测数据。

## 数据约束

- `data/` 只读，不修改、重命名或覆盖原始 MAT 文件。
- 主分析只使用 Fz、F3、F4 原始 EEG；FzDecon、F3Decon、F4Decon 只可作设备输出对照，不能作真值或替代主输入。
- MAT 中 `data` 的实际形状为 `10 × N`。任何加载器必须校验变量名、形状、标签、采样率和时间戳，不得依赖“第一个非系统变量”等脆弱规则。
- 事件解释以 `docs/DATA_SPEC.md` 为准。第 9 通道在 Task-1 与 Task-2 的实际编码不同，禁止统一按 ±2 解析。
- A/B 的真实身份、EEG/ECG 幅值单位、Task-1 第 9 通道的精确语义尚未由权威资料确认；未知项不得写成事实。
- 三个额区通道不足以唯一定位深部源。只能报告有效神经状态、有效投影或机制假设，不能声称恢复了真实 LGN、海马或高分辨率皮层源。

## 开发门禁

每个问题编写主要代码前，先在 `paper/` 完成对应数学建模文档，至少包含：目标、假设、符号、公式、参数估计、算法、验证和局限。

实现时：

- 先查找并复用现有实现；只改任务直接相关文件，避免无关重构；
- 固定随机种子，保存环境、配置、命令和输出路径；
- 所有数据变换在训练折内拟合；禁止用测试试次构造模板、阈值、标准化参数或特征窗口；
- 行为中的错误、缺失、超时和运动响应分别编码；运动后信息不得进入认知前缀预测；
- 非平凡逻辑至少保留一个可运行的最小检查；
- 输出写入 `code/` 或 `paper/` 的明确子目录，不污染 `data/`。

## 实验与论文证据规则

- 假设、仿真、真实数据和实测结论必须分开标注。
- 每个结果数字、表格和图件必须能追溯到配置、代码和实际输出。
- 未运行的实验使用“待完成/计划/候选”，不得使用“结果表明”“验证成功”等措辞。
- P300 仅称为“候选 P300/任务相关正向成分”，除非电极覆盖和结果支持更强结论。
- 分类性能只能证明条件可区分，不能单独证明神经机制。
- 论文发布前检查问题—方法—证据—结论链、符号、单位、引用、图表编号和附录复现命令。

## Skills 使用

- 学术写作使用 `.agents/skills/academic-writing-skills/`，遵守证据范围和候选稿检查。
- 科研绘图仅在有明确科学问题、真实数据和已确认图件计划时使用 `.agents/skills/academic-figure-skill/`。
- 只有现有 Skill 无法覆盖且需求会重复出现时，才考虑创建新 Skill。

## 会话结束与恢复

每个阶段、建模问题或重要里程碑结束时，同时更新：

- `docs/PROGRESS.md`：状态、实际验证、决策、阻碍、风险和下一步；
- `docs/PLAN.md`：任务状态和方案变更。

状态只能使用：未开始、进行中、已完成、受阻、待验证。只有达到文档中验收标准后才能标记“已完成”。
