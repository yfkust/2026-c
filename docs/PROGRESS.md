# 项目进度

最后更新：2026-09-24（Asia/Shanghai）

## 当前阶段

阶段 1：数学建模与方案设计仍**进行中**（问题一已完成，问题二、三未开始）；阶段 2 **进行中**，问题一事件/QC/划分及 B0 最小代码已完成；阶段 3 **进行中**，本次仅完成 B0 频响及纯合成先行验证。真实 EEG 保真、正式 ERP、分类与 C1 模型选择均未开始。

阶段 0 的 5 个可验收任务均已完成：目录与资料定位、数据结构核验、事件与质量审计、代码与环境审计、项目文件落盘及状态同步。阶段0结束时尚未建模；本次已完成问题一设计，代码实现与正式实验仍未开始。

## 整体进度

| 阶段 | 状态 | 已达到的里程碑 |
|---|---|---|
| 0 项目审计与需求分析 | 已完成 | 5/5 阶段任务通过 |
| 1 数学建模与方案设计 | 进行中 | 问题一设计已完成；问题二、三未开始 |
| 2 模型代码实现 | 进行中 | 问题一前置事件/QC/划分和 B0 FIR/基线单元已完成；ERP、C1及其余实现未开始 |
| 3 实验验证、可视化与优化 | 进行中 | B0 频响与18个冻结纯合成场景完成；真实 EEG/半模拟/ERP/C1 验证未开始 |
| 4 论文写作与最终复核 | 未开始 | 无 |

## 阶段 0 历史完成记录（保留）

### 创建或修改

- 修改 `AGENTS.md`：用长期项目约束替换误放的 q1 草稿；原内容与 `docs/q1.md` 完全相同，研究稿仍保留。
- 创建 `docs/PROJECT_BRIEF.md`：三问的目标、输入、输出、候选方法、指标、假设和验收标准。
- 创建 `docs/DATA_SPEC.md`：MAT 结构、事件语义、质量事实、冲突和待确认事项。
- 创建 `docs/PLAN.md`：五阶段、三问里程碑、跨问题接口、风险和当前状态。
- 创建 `docs/PROGRESS.md`：阶段台账和会话恢复点。
- 创建 `paper/paper.md`：按 `docs/deadline.md` 建立带待完成标记的论文扩展目录。

### 已验证事实

- 核心目录均存在；格式文件实际路径为 `docs/deadline.md`，不是根目录 `deadine.md`。
- `docs/` 含原始赛题 DOCX、Markdown 提取稿、整合解题思路、q1/q2/q3 草稿、方法摘要和格式说明；原始 DOCX 的关键数据说明已用 OOXML 抽取核对。
- `refer_paper/` 含 6 篇 PDF 和一份方法精炼；`repos/` 含 wICA/ICA 的 MATLAB 参考实现、MNE-Python 源码快照和 PyWavelets 阈值资料。阶段 0 只检查入口和相关性，没有无目的地全量审读 140 MB 参考仓库。
- 四个 MAT 均为 MATLAB v5，包含 `SampleRate`、`DataLabel`、`data`；实际数据方向为 `10 × N`，采样率均为 256 Hz。
- 时间戳严格以 1/256 s 递增；四份记录各有 100 个 cue，共 400 个。
- Task-1 第 9 通道为持续 ±1；Task-2 为持续 ±1 加单样本 ±2，不能统一解析。
- 原始 EEG 存在连续的精确 ±1000 限幅样本；各文件/通道比例已写入 DATA_SPEC。
- A/B 身份、物理单位和部分事件语义无法由现有权威资料确认。
- `code/` 只有 README，尚无项目实现；`paper/` 原为空。
- Python 可用，SciPy/NumPy/pandas/matplotlib/scikit-learn/statsmodels 已安装；MNE、h5py、PyWavelets 未安装；项目根目录不是 Git 仓库且没有环境锁文件。
- 当前 matplotlib 默认配置目录不可写，会自动退回 `/tmp`；阶段 2 应显式设置项目专用 `MPLCONFIGDIR`。系统可找到 LibreOffice 和 Git，未找到 MATLAB、Octave、R 或 Pandoc。
- `.agents/` 提供学术写作和科研绘图 Skills；本阶段使用学术写作约束规划论文，未生成科研图。
- `docs/deadline.md` 给出了论文目录、摘要 300–1000 字且不超过一页、3–5 个关键词等要求，但未发现可执行的最终字体、页边距或提交模板参数；这些格式项仍待官方规则确认。

### 实际执行的检查

- `rg --files`、`find`、`file`、`du`、`sha256sum`、`cmp`；
- Python/SciPy `whosmat` 与 `loadmat`；
- NumPy 对形状、有限值、时间戳、事件转移、run 长度、响应延迟和限幅连续段的统计；
- Python 模块导入检查和命令行运行时检查；
- DOCX OOXML 文本抽取，确认原题数据说明与 Markdown 提取稿关键段落一致。
- 最终断言脚本复核四份 MAT 的形状、采样率、有限值、时间步长、cue 数与第 9 通道值域，输出 `phase0_audit_assertions: PASS`；随后重算四份原始数据 SHA-256，与 `docs/DATA_SPEC.md` 一致。

以上均为只读审计或文档创建，不是模型实验。

## 阶段 0 关键决策（历史）

1. 资料权威顺序固定为原始赛题 > 原始数据/复核事实 > 整合稿 > 其他解读与参考资料。
2. 阶段 1 先完成问题一数学文档，阶段 2 才编写主要代码。
3. 采用“简单基线先行、留出证据支持后再升级”的方案；结构化分解、神经场和完整 DDM 均不是默认必选。
4. 三问共享同一事件表、质量掩码和数据划分，避免各问独立处理造成口径不一致。
5. Task-2 的 cue 形状方向、目标位置和点击方向分开；深部结构只作机制假设/有效状态解释。

## 阶段 0 结束时未完成任务（历史）

阶段 0 无未完成交付物。以下是已登记、留待后续阶段解决的事项：

- 事件语义的操作性协议和人工波形抽样尚未冻结；
- 工频、频谱、平直段、突跳、ECG 耦合和 trial 级质量尚未计算；
- 三问数学模型、参数、划分和验证协议尚未定稿；
- 未安装后续可能需要的最小依赖，未创建任何模型代码或实验输出；
- 未获得 A/B 身份、单位、设备限幅或超时规则的额外元数据。

## 关键风险

- 限幅持续最长约 2.10 s，可能覆盖完整事件窗口并显著减少有效 trial。
- 三个额区通道对经典中央—顶区 P300、眼动混杂和深部源均缺乏充分空间信息。
- Task-1 行为事件语义不清，错误/RT 解释若处理不当会直接污染问题三。
- 整合方案的方法链较复杂；在 400 个 trial 上同时拟合高维神经场和完整认知模型可能不可识别。
- 旧 q1 示例按错误矩阵方向读取数据，后续不可直接复用。

## 阶段 0 结束时下一步任务（已由本次完成）

进入阶段 1，完成 `paper/model_q1.md`。目标是把问题一固定为可实现、不可泄漏的数学方案，包括事件协议、观测模型、质量掩码、温和滤波基线、一个保特征候选方法、曲线拟合、半模拟与留出验证。

验收标准：公式变量全部映射到 DATA_SPEC；训练/验证边界明确；每个候选方法有采用或淘汰条件；P300、饱和和混杂结论边界清楚；尚无实验结果的部分全部标记为计划。

## 阶段 0 模型建议（历史）

推荐 **GPT-6 Astra**。下一任务涉及 ERP 信号处理、统计验证、滤波失真、数据泄漏和可辨识性的联合设计，是后续全部代码的数学契约，错误成本高。可将后续“按已冻结方案实现 MAT 加载器与事件表”拆给 **GPT-6 Sol Medium**，但模型假设和验证协议应先由 Astra 完成一致性审查。模型切换由用户决定。

## 会话恢复提示

新会话读取 `AGENTS.md`、`docs/PLAN.md`、`docs/PROGRESS.md` 后，按任务阅读 `paper/model_q1.md`、`paper/b0_validation.md` 与相关代码。问题一事件/QC/划分、B0 最小实现及其频响/纯合成验证已完成；下一步按用户选择先做盲于标签的真实训练片段诊断，或完成问题二、三设计。不要重建历史文档、重跑阶段0审计，也不要把两处 superseded 输出用于新分析。

## 本次里程碑：阶段 1 问题一（2026-09-24）

### 实际完成的文件与内容

- 新建 `paper/model_q1.md`：13节完整研究协议，公式(1)–(16)、符号、cue/run语义、整数时间窗、硬/软QC、有限支持滤波、基线、B0/C1、ERP、自然样条、候选峰和时频特征、分块嵌套验证、伪代码及问题二接口。
- 修改 `docs/PLAN.md` 与本文件，保留阶段0历史成果。未修改 DATA_SPEC、原始数据、旧研究稿或论文骨架。
- 沿用已核实10×N、256Hz、400cue和限幅统计，没有重复MAT审计。识别并澄清“边界可用”与“质量通过”的差别。
- 核验SciPy官方FIR/DCT约定和与滤波失真、变换域提取、眼动混杂直接相关的原始研究记录；来源列于model_q1第13节。

### 设计验收与待验证的区别

| 任务 | 状态 | 验收或缺口 |
|---|---|---|
| 事件协议与整数索引 | 已完成 | cue-only，Task-1行为字段null，窗口/持续/异常run定义完整 |
| QC与去噪数学方案 | 已完成 | 饱和不插值、共享支持、训练QC、B0及闭式C1、失败回退 |
| ERP、拟合与特征 | 已完成 | 均值/样条/窗口特征有输入输出和参数规则，允许无峰 |
| 划分、指标及接口 | 已完成 | 连续块purge、训练范围、负对照、模拟/真实边界和可追溯接口 |
| 实现正确性及方法有效性 | 待验证 | 未编写管线、未进行正式实验或性能评估 |
| 阶段1问题二、三设计 | 未开始 | 本次不在范围内 |

### 关键决策与风险

B0采用有限支持低通与基线，低频高通仅作满足安全支持条件的敏感性；C1将整合稿的保护思想落实为训练折共享的有界DCT增益，允许ρ=0。其能量界不等于神经保真，必须通过半模拟与留出保真门槛。用自然样条避免强行拟合P300峰；去噪选择不以分类率最大化。

仍待确认：实际保留trial数、物理单位、A/B身份、Task-1行为语义、触发显示延迟、眼动混杂、QC阈值、滤波长度、收缩强度与模拟容差。严格支持规则可能使部分折样本不足；不能为了可发表结果而事后放宽。阶段0的100个完整窗口不是去噪有效样本量。

### 下一步最小可验证任务与模型建议

建议在用户授权进入实现后，仅完成事件表、硬/软质量标记和支持感知的分块划分，拟新增 `code/q1_audit.py` 与最小检查文件。验收：各记录100 cue、时间/索引一致、限幅拒绝符合规则、跨折输入交叠为零、测试值修改不影响训练阈值；报告真实保留数及失败原因，不跑正式分类/ERP实验。

推荐 **GPT-6 Sol High，high档位**：该单元逻辑规模小，但边界索引、缺口膨胀、训练折统计和防泄漏需要细致跨模块检查。可将已明确的字段整理/日志汇总拆给GPT-6 Luna medium。当前不必升级Astra high；若严格QC几乎无数据或需要引入带缺失观测的联合漂移/伪迹分解，Astra high应专门解决可辨识性与选择偏差，而非只增加模型复杂度。模型切换由用户决定。

### 本次验证范围

进行设计级交叉审查：公式维度、整数端点、掩码支持、参数估计归属、无峰/缺类/零方差分支、来源与结论范围。`rg -n '^(##|###)' paper/model_q1.md`核对13节结构。命令行Python用固定向量检查307/512点网格、`[77:384]`裁剪、W2的65点、FIR支持576/640点、DCT逆变换及式(13)能量界，输出 `Q1 mathematical-spec checks PASS`；没有加载EEG，没有评价算法性能。解析证明仍以model_q1第6.3节为准，单个数值例子不替代证明。

最终候选文档SHA-256：`9d9c41f1d1dcfd728fbfdb7cbd27601cf80b98b8c45250037f0ce0e837e10538`（`sha256sum paper/model_q1.md`）。按研究目标→处理模型→可计算指标→推断限制人工复核，结论范围符合设计阶段；参数有效性及正式论文发布检查未完成。无正式实验结果，无科研图，无新增依赖。学术写作Skill用于维护证据状态及跨文件一致性，不表示方法已通过实证检验。

## 本次里程碑：阶段 2 问题一前置数据单元（2026-09-24）

### 当前阶段与已完成任务

- 仅实现 `paper/model_q1.md` 的事件协议、硬/软质量标记和外层固定划分，未进入问题二、三，不运行正式去噪/ERP/分类实验。
- 新建 `code/q1_audit.py`：只读加载四份 MAT，校验 10×N、256 Hz、标签、有限值和时间步长；以 VisCue 的 ±1 run 起点定义 cue；保留 Task-1 第9通道原始 run 且标未知语义；输出 Task-2 的 ±2 候选 marker；采用 `[o−160:o+416)` 原始支持；标记精确 ±1000/越界、≥64点平直和状态通道交叠；在外训练折估计软 QC 中位数/MAD；连续20-trial测试块、邻接purge1及支持交叠排除。
- 新建 `code/test_q1_audit.py`：3个单元测试覆盖 cue/run、Task-1 未知行为、跨任务 ID 不碰撞、长短限幅、原始支持不交叠、测试原始值和质量指标变化不影响训练阈值。
- 新建有效输出 `code/outputs/q1_audit/`：`events.csv`、`quality.csv`、`splits.json`、`summary.json`、`manifest.json`，清单保存命令、配置、Python/NumPy/SciPy 版本、原始数据和脚本 SHA-256。第一次运行暴露跨任务 `trial_id` 碰撞；已在生成源头修复，并将第一次无效输出完整移至 `code/outputs/q1_audit_superseded_bad_ids/`，不可用于后续研究。修改 `docs/PLAN.md` 和本文件同步状态。未修改 `data/`。
- 对 `paper/model_q1.md` 第11节作接口一致性修订：软 QC 通过/拒绝是折特异量，保存在 `splits.json`，不是 `quality.csv` 的全局布尔列；当前审计版尚无内层验证折。先前记录的设计稿 SHA-256 是修订前历史值；当前文件 SHA-256 为 `59a90fb360444506d45dc405141dc659adb99a9795307898114f4aa59eaa6130`。

### 实际运行与复核

- 命令：`python -m unittest discover -s code -p 'test_q1_audit.py' -v`，3/3通过；`python code/q1_audit.py --data-dir data --output-dir code/outputs/q1_audit`，退出码0。
- 单独读取现行 CSV/JSON 作输出完整性断言，结果 `q1_output_integrity: PASS`：400个 cue 的 ID 全局唯一、1200条通道质量记录、各记录100 cue/5折；硬通过数与摘要一致；训练/测试/purge ID 不交叉，所有训练与测试原始支持区间不重叠，QC拟合ID仅来自训练。
- 实测审计计数（非模型性能）：A1、A2、B1、B2 的硬质量通过数分别为 **85、88、89、84 / 100**；对应5折外层测试质量通过数合计分别为 **82、86、83、83 / 100**。各记录 `cue_anomalies=[]`，`state9_overlap=0`。硬拒绝的 `clip_or_out_of_range` trial 数分别为15、12、11、16；`flat_run`数13、9、8、12，两类可重叠，不能相加当总拒绝数。软 QC 额外拒绝可由每折 `trial_reasons` 追踪。
- 无正式 ERP、P300、左右判别、滤波保真、显著性或神经机制结果；κ=6、平直64点、H=32 仍是待验证工程候选。Ponytail Skill 促使本次只保留最小可验证数据单元和一个测试文件，未创建额外框架或依赖。

### 未完成任务、关键风险与下一步

- 阶段2问题一仍未完成：B0有限支持FIR、基线、ERP、C1与全部科学验证未实现；阶段1问题二/三设计未开始。阶段3/4未开始。
- 严格质量规则下总外层测试通过334/400，样本减少可能削弱左右条件和后续跨记录分析；当前只证实规则执行，不证实阈值合理、视觉响应保真。Task-1第9通道、物理单位与A/B身份仍未知；Task-2 marker 仅按候选语义保存。
- 下一最小任务：在 `code/q1_preprocess.py` 与最小测试中实现 B0 的65-tap有限支持 FIR、单 trial 基线、三通道输出，复用现有事件/质量/划分文件。验收为支持索引正确、合成已知波形的幅值/潜伏期误差可复核、被硬拒绝trial不输出伪造波形、测试标签不参与拟合；只作单元和小样本检查，不进入正式 ERP/分类实验。模型建议 **GPT-6 Sol High，medium档位**：主要是索引、有限支持和失真核验，现有数学协议已固定；若出现滤波与饱和边界的复杂冲突再调至high。文档整理可拆给Luna，暂不需要Astra；仅当质量保留/失真使B0假设本身不可行时，才考虑Astra High审查可辨识性与替代观测模型。

## 本次里程碑：阶段 2 问题一 B0 最小实现（2026-09-24）

### 已完成与关键决策

- 新建 `code/q1_preprocess.py`：只读复用有效审计版 `events.csv`、`quality.csv`、`splits.json` 与来源哈希；固定 `firwin(65,30,fs=256,window='hamming')`，在 `[o−160:o+416)` 作一次居中有效卷积，得到 512 点扩展波形；以相对索引 −51…−1 的 51 点均值逐 trial/通道校正，裁 `[77:384]` 输出 307 点核心波形。无高通、notch、ICA、C1、训练模板或标签驱动滤波。硬故障支持内任何样本导致拒绝，不插值。
- 新建 `code/test_q1_preprocess.py`：3项合成契约测试覆盖支持首末点和直接求和、ERP可用形状/时间基线、幅值/潜伏期/波形/50 Hz残留、饱和拒绝及支持外数据变动不影响波形。
- 新建 `code/outputs/q1_b0/epochs_b0.npz` 与 `manifest.json`：保存 346 个硬通过 trial 的三通道 `346×3×307` B0 波形及永久 ID、记录、cue 标签、时间轴、基线常数、外层测试折/质量标记、FIR taps；manifest 保存命令、配置、依赖版本、审计文件与脚本 SHA-256。该 NPZ 是硬通过候选集合，不是直接可整体平均的 ERP 集合；后续训练/测试 ERP 必须与 `splits.json` 的折内质量通过 ID 交集。
- 修改 `docs/PLAN.md` 与本文件同步状态。本次只使用既有 NumPy/SciPy，无新依赖；Ponytail Skill 使实现保持为单个固定算子和一个测试文件，没有预建 ERP 或通用流水线。

### 实际测试结果与命令

- `python -m unittest discover -s code -p 'test_q1_*.py' -v`：全部 **6/6 通过**（旧审计3项、新 B0 3项）。
- `python code/q1_preprocess.py --data-dir data --audit-dir code/outputs/q1_audit --output-dir code/outputs/q1_b0`：退出码0；各记录硬通过波形数 **85、88、89、84**，外层测试质量通过 **82、86、83、83**，与前次审计一致。此为预处理输出，不是正式 ERP 实验。
- 独立读取 NPZ/JSON 检查：`b0_output_integrity: PASS`；346 个唯一 ID、波形形状 `(346,3,307)`、时间轴 `k/256 (k=−51…255)`、外层测试通过总数334、逐折通过 ID 与审计 `test_quality_pass` 完全一致且不在该折训练 ID 中；核心波形51点基线均值的最大绝对残差为 `2.77×10^−13` RU。`b0_manifest_hashes: PASS`，审计与脚本哈希匹配。
- 单一预设合成测试场景（非真实 EEG 保真结论）：4 RU、高斯宽度12样本、中心 cue 后90样本，外加另一通道50 Hz正弦。实际 B0 峰值误差 `+0.00288843` RU、峰时误差 `0` 样本、波形 NRMSE `0.000827647`、50 Hz残留 RMS `0.00103657` RU。最初错误假设“低通峰值必下降”的测试断言失败，按实测修正为幅值误差绝对值界；轻微正增益说明零相位也不等于峰幅不变。该场景仅用于代码契约检查，不能推广到真实多成分波形。
- `git diff --check` 退出码0；未修改 `data/`，未运行正式 ERP、分类、C1 或问题二、三实验。

### 未完成、风险与下一步

- 尚未做 B0 全频响、不同波宽/多峰/噪声/边界场景保真、真实数据盲抽波形、质量阈值敏感性；FIR 30 Hz和 QC 参数仍为候选。记录单位未知；三额区通道不能保证眼动混杂已消除。非因果 B0 不可直接当实时脑机接口前缀输出。
- 下一最小任务：冻结纯合成场景清单，计算 B0 的 1–20/30/50/60 Hz 实际增益以及多场景幅值、潜伏期、NRMSE，保存可复算验证记录；验收为各场景生成参数/种子/真值明确，数字由脚本重建，异常或失真不能被单场景平均掩盖。本任务之前不做真实 ERP 或 C1 选择。
- 推荐 **GPT-6 Sol High，medium档位**：滤波/合成验证需要细致时域—频域一致性审查，但既有公式和代码均固定；场景登记、日志整理可用Luna。暂不需要 Astra High；若多场景显示B0严重扭曲慢成分或严格QC使有效试次不足，再由Astra High审查替代信号模型与可辨识性。

## 本次里程碑：阶段 3 问题一 B0 频响与纯合成验证（2026-09-24）

### 当前任务、文件和实际测试

- 新建 `code/q1_b0_scenarios.json`：在首次完整运行前冻结18场景×100次、主种子20260924、正负/多峰/无峰、不同宽度与幅值、潜伏期抖动、AR(1)、50/60Hz、混合噪声、0.2Hz漂移、宽瞬态及支持内外限幅。最终配置SHA-256为 `192f17a71a9aa882094f02b5f82fe1093b84ef4b7cf4d8effe5dffedac65ec84`；模拟观测含`10⁻⁶ RU`传感器抖动防止人为零平直误判，真值不含抖动。
- 新建 `code/q1_b0_validate.py` 和 `code/test_q1_b0_validate.py`，复用现有 `b0_epoch`/`hard_mask` 与有效B0输出中的实际taps，未修改旧模块。新建 `code/outputs/q1_b0_validation/`：频响曲线/精确摘要、逐试次与逐场景CSV、`18×100×3×307` 干净真值、PDF/300dpi PNG 图件及manifest；新建 `paper/b0_validation.md` 记录方法、结果、图件QA与推断边界。修改 `docs/PLAN.md` 和本文件，未修改 `data/`。
- 命令：`python -m unittest discover -s code -p 'test_q1_*.py' -v`，**9/9通过**；`MPLCONFIGDIR=/tmp/q1_b0_mpl XDG_CACHE_HOME=/tmp/q1_b0_cache python code/q1_b0_validate.py --config code/q1_b0_scenarios.json --b0 code/outputs/q1_b0/epochs_b0.npz --output-dir code/outputs/q1_b0_validation`，退出码0；科研绘图Skill `qa_validator.py` 退出码0。独立核对manifest输入/脚本SHA、54条场景×方法摘要、14400条有效通道-试次-方法指标、真值形状和限幅拒绝，输出 `b0_validation_integrity: PASS`。频响PDF/PNG、冲激PDF/PNG均存在，PDF字体嵌入；图已目视检查。
- 首轮频响通带放大图纵轴过宽，属于**图表呈现问题**而非数据/方法失败；仅修正显示尺度后用同一配置重跑，旧版保留在 `code/outputs/q1_b0_validation_superseded_plot_v1/`。两轮 `trial_metrics.csv` 与 `scenario_summary.csv` 的哈希逐一相同，不得使用旧图作为当前稿图件。未按观察结果改变场景或判断阈值。

### 已验证数值与不满足预期的场景

- 65 tap实际系数和=1、对称最大误差`5.21×10⁻¹⁸`；1–20Hz增益`0.999697–1.002265`、波动`0.022287dB`；30Hz`−6.006887dB`（约半幅，非−3dB），50Hz`−56.671865dB`，60Hz`−62.438661dB`。
- 八个低干扰平滑高斯/多峰/潜伏期抖动场景中，B0-clean带宽NRMSE中位数范围`0.000317–0.001075`，B0峰时误差中位数均为0采样。50/60Hz固定正弦噪声场景中，B0中位NRMSE分别约`0.00129/0.00197`，但不能外推到实际工频复杂性。
- **方法限制，不是测试失败**：AR(1)、SNR0.3下B0中位NRMSE`2.286`、峰时误差`6.5`采样；混合SNR0.1下NRMSE`4.541`、误差`10`采样；0.2Hz漂移B0与raw均约`1.616`；宽瞬态B0`3.175`略差于raw`3.174`。B0没有消除低频/宽瞬态，对强有色噪声保真不足，不能靠修改阈值宣称通过。
- 无峰白噪声的固定输入尺度阈值触发率raw`10.33%`、B0`0%`，阈值未按输出噪声等灵敏度标定，不作神经假阳性优越性结论。支持内单点/80点±1000均100/100拒绝且无重建指标；支持外限幅0/100拒绝。完整18场景的幅值、潜伏期、NRMSE、相关、RMSE与拒绝率见`scenario_summary.csv`。
- 本次纯合成以程序已知干净信号为真值；没有真实EEG无噪声真值、正式ERP、分类、C1或问题二/三结果。

### 状态判断、风险与下一步

- B0 **代码与复现条件已具备**；但 AR/混合/漂移/瞬态失败模式是否存在于真实数据、以及眼动和严格QC选择偏差尚未评估，因此 **尚不具备无条件真实ERP推断条件**。若后续进行描述性ERP，须先披露这些限制并完成真实数据盲诊断。
- 下一最小任务：仅对既有外层训练集合做盲于cue标签的原始/滤后波形抽样、训练片段PSD、漂移/瞬态代理及逐条件质量保留数；复用审计/B0输出，不用测试折选择参数，不求正式ERP/分类。验收：抽样规则、每图输入支持、频谱参数、计数和命令可重建，并明确哪些纯合成失败模式与真实数据相符。
- 推荐 **GPT-6 Sol High，high档位**：真实数据诊断要同时约束有限支持、时间块隔离、功率谱定义和伪迹解释，较常规日志汇总更易出现隐性泄漏或误判。图表排版/台账可拆给Luna medium。若诊断显示大量真实试次被低频/宽瞬态主导且B0不可用，才考虑 **Astra High** 专门审查含缺失观测的替代模型、可辨识性与选择偏差；当前不自动切换。
